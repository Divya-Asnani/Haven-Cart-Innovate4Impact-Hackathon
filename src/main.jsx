import React from 'react'
import ReactDOM from 'react-dom/client'
import HavenCart from './HavenCart.jsx'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <HavenCart />
  </React.StrictMode>,
)
